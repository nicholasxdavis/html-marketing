# Specs

Do not invent sizes, icons, or store rules. Look them up.

```bash
npx html-marketing sizes
npx html-marketing sizes youtube
npx html-marketing specs platforms
npx html-marketing specs copy
npx html-marketing specs icons
npx html-marketing prompts atlas
```

Design system: `DESIGN.md`. One style prompt per set.
